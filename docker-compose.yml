services:
  bookeeper:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bookeeper
    restart: unless-stopped
    ports:
      - "9999:9999"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    environment:
      - TZ=Asia/Shanghai
